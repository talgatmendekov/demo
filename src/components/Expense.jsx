import React from 'react'
import { ExpenseItem } from './ExpenseItem'

const Expense = (props) => {
  return (
    <div>
        <ExpenseItem/>
        <ExpenseItem/>
        <ExpenseItem/>
    </div>
  )
}

export default Expense